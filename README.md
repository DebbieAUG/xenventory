# xenventory

A simple static web project scaffold for an inventory management application.

## Structure

- `index.html` for the landing page
- `dashboard.html`, `inventory.html`, `requests.html`, and `approvals.html` for main views
- `css/styles.css` for shared styling
- `js/` for app logic modules
- `assets/` for images and icons
